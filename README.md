# ranag542-ai-image-generator
A simple command-line tool to generate multiple images from a text prompt using Stable Diffusion (via Hugging Face Diffusers). Supports GPU acceleration for faster image generation.
